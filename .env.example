# NestJS taskboard runtime configuration.
# Copy to .env (or export in the platform environment) and adjust values.
# None of these values are secrets; there is nothing sensitive to commit.

# Server
PORT=8080
BIND_HOST=0.0.0.0

# Persistence. DATA_DIR/DATABASE_PATH point at the persistent SQLite file.
# Keep this path on a persistent volume; the app never stores permanent
# data in an ephemeral release directory.
DATA_DIR=./data
DATABASE_PATH=./data/taskboard.db

# Non-sensitive release marker shown in the UI footer via /api/meta and
# resolved by scripts/build.sh into VERSION. BUILD_MARKER overrides git SHA.
BUILD_MARKER=develop